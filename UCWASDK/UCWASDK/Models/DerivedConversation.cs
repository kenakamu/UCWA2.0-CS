namespace Microsoft.Skype.UCWA.Models
{
    /// <summary>
    /// Represents a related conversation with a different participant than the one of the original conversation.
    /// A derivedConversation is created when a new modality is added to an existing conversation but is answered by another participant (perhaps due to simultaneousRingSettings). 
    /// </summary>
    public class DerivedConversation : Conversation
    {       
    }
}
