namespace Microsoft.Skype.UCWA.Enums
{
    /// <summary>
    /// Represents when call forwarding settings are active.
    /// </summary>
    public enum ActivePeriod
    {
        /// <summary>
        /// Call forwarding settings are always active.
        /// </summary>
        Always,
        /// <summary>
        /// Call forwarding settings are active only during workhours.
        /// </summary>
        Workhours
    }
}
